# wordcount-project was a project to count the number of words a user entered.
now working on portfolio-project folder 
but terminal doesn't save and says it's not a repository ( git add -A command )
